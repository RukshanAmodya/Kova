document.addEventListener('DOMContentLoaded', () => {
    console.log('Kova Admin Dashboard loaded.');
    
    // Placeholder values
    document.getElementById('total-videos').textContent = '12';
    document.getElementById('active-users').textContent = '148';
});
